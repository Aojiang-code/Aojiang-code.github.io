# 01Automatic_heart_sound_classification_from_segmented-unsegmented_phonocardiogram

我对这篇文章的评价，这篇文章已经过时了。
这篇文章2020年发表，所以，这篇文章中介绍的机器学习的方法和模型，绝大多数都是2019年之前的，要知道，2020年，深度学习已经开始有爆发趋势，到2023年人工智能已经成为热点话题，但由于这篇文章是2020年的，所以这篇文章的介绍依旧停留在传统的机器学习上，只有很少的篇幅在介绍LSTM模型。
当然咯，本文将传统的机器学习方法与深度学习方法进行对比，本身也是本文的一大特点。

> 一句话概括：
> 两个对比研究：
> * 传统机器学习(包括支持向量机（SVM）、K近邻（KNN）、决策树、集成分类器、人工神经网络（ANN）)与LSTM之间的对比：结果发现LSTM的模型分类效果更好
> * 心音(PCG)进行分割与不进行分割之间的对比：结果发现分割后的PCG的模型分类效果更好
> 
> 结论为：使用时频域特征(MFCC)可以提高分类模型的准确性。
>
> 另外，补充一句，该研究中还发现，与传统的机器学习模型相比(highest AUC score is 62.08% with decision tree classifier)，使用未分割的PCG的MFCC特征在LSTM中的预测效果更好(AUC score of 91.39%)，所以从这一点可以得出一个结论：传统机器学习(包括支持向量机（SVM）、K近邻（KNN）、决策树、集成分类器、人工神经网络（ANN）)与LSTM之间的对比：结果发现LSTM的模型分类效果更好。**这一点提示我，如果我要做PCG相关的研究，应该直接提取MFCC特征后，使用LSTM模型。**
> 此外，上述描述中提到，使用未分割的PCG，传统的机器学习模型 highest AUC score is 62.08% with decision tree classifier，但是在传统的机器学习模型中使用分割后的PCG，over 70% of AUC scores，所以从这一点可以得出一个结论：心音(PCG)进行分割与不进行分割之间的对比：结果发现分割后的PCG的模型分类效果更好。**这一点提示我，如果我要做PCG相关的研究，应该对PCG进行分割。**
>
> 使用了公开数据集：[2016年PhysioNet/Computing in Cardiology Challenge](https://archive.physionet.org/challenge/2016/papers/)
> 我没找到相关的数据集
> 这个平台的官网为：https://physionet.org/
> 我在这个平台的官网上搜索PCG，发现了[Heart Murmur Detection from Phonocardiogram Recordings: The George B. Moody PhysioNet Challenge 2022](https://physionet.org/content/challenge-2022/1.0.0/)这个数据集
> 此外，还发现了一个数据集，该项目收集了来自不同孕妇在单胎生理怀孕最后几个月(孕周在31至40周之间)的一系列26个胎儿心音图(PCG)信号。胎儿 PCG 数据库:[Fetal PCG Database](https://physionet.org/content/fpcgdb/1.0.0/)


## 梗概
这篇论文的核心内容是关于自动心音分类的研究，使用分割和未分割的心音图（Phonocardiogram，PCG）信号，并利用时间和频率特征进行分析。以下是该研究的关键点：

1. **研究目的**：利用心音信号检测心脏异常，这在几十年来一直是研究的活跃领域。

2. **方法**：研究中深入分析了不同的时间和频率域特征，并实验确定了有效的特征子集以提高分类性能。研究了分割和未分割的PCG信号，并报告了相关特征子集及其分类性能的重要结果。

3. **分类算法**：使用了不同的分类算法，包括支持向量机（SVM）、K近邻（KNN）、决策树、集成分类器、人工神经网络（ANN）和长短期记忆网络（LSTM）。

4. **主要结果**：观察到LSTM在从未分割PCG数据中提取的Mel频率倒谱系数（MFCC）特征上表现更好，AUC得分为91.39%。然而，MFCC特征在其他分类器上并未显示出一致的性能。相比之下，对于分割数据的时间-频率特征，所有分类器的性能都是可观的，AUC得分均超过70%。

5. **研究意义**：结果强调了时间和频率域特征的重要性，并得出结论，为了实现改进的分类，需要同时使用分割PCG信号的时频特征。

6. **数据集**：研究使用了2016年PhysioNet/Computing in Cardiology Challenge提供的超过3000个心音记录的公开数据库。

7. **特征提取**：从分割的PCG信号中提取了50个特征（20个时域和30个频域特征），并从未分割的PCG信号中计算了MFCC特征。

8. **分类性能**：使用不同的特征集对分类器的性能进行了评估，包括真正例（TP）、真负例（TN）、假正例（FP）和假负例（FN），并计算了敏感性（Sensitivity）、特异性（Specificity）、准确性（Accuracy）、整体得分和曲线下面积（AUC）。

9. **未来工作**：作者计划开发一个PCG信号数据库，并应用深度学习方法，包括卷积神经网络（CNN）和双向LSTM，以及研究新特征，如Hilbert变换（HT）、离散小波变换（DTW）和功率谱密度（PSD）。

这篇论文提供了心音分析领域的一个全面研究，包括信号预处理、特征提取、分类器设计和性能评估，旨在提高心脏异常检测的准确性。


## 详述
### 1. INTRODUCTION

心脏是一个重要器官。
监视心脏活动可以避免一些严重的疾病。

心音包含了很多信息，这些信息有助于识别心脏异常。

心脏瓣膜的异常和血流的加速/减速会产生明显的杂音，然而这些杂音不容易听到。

此外，只有经验丰富且为高水平的心脏专家才能通过心音分辨心脏的正常与异常。

据美国CDC报道，全球有超过一百万的人由于缺乏廉价且可靠的诊断与筛查而面临因心脏疾病而导致的死亡或不得不面对严重的心脏疾病。

在过去的几十年里，机器学习和信号处理一直在促进医学专家加速和改进心音听诊过程。
在世界范围内，已经有许许多多的研究者致力于研究心音分类模型的各种方法。具体方法如下所示：

![表一](01图片/01表1.png)

> 上表其实都是一些传统的机器学习方法，不看也罢，如果你感兴趣的话，可以看看，都理解模型与逻辑有帮助，但是，现在的热点是深度学习，所以，为了节约宝贵的时间精力，不建议看上表相关的内容。


在深度学习的模型中，心音常常不进行分割，而是完整地作为深度学习模型的输入，由此，引出本文的研究内容：
本文对以往心音研究的文献进行了综述，讨论了相关的分割、特征提取和分类技术。

> 下面作者开始了他的综述内容：

关于心音信号（PCG）的预处理、分割和分类的综述。它讨论了在噪声干扰下，如何通过不同的信号处理技术来提高心音信号的分析和分类的准确性。

主要讨论了心音信号（PCG）分析中的噪声问题、预处理技术、信号分割方法以及特征提取和分类技术。以下是核心内容的总结：

1. **噪声问题**：周围噪声可能导致心音信号中的S3和S4被误解，这些心音是由血液冲击左心室和心房收缩产生的。

2. **预处理技术**：
   - 使用**Systole Width Estimation (SWE)**和**R-R Interval Estimation (RRE)**来区分有效的S1/S2对。
   - 应用基于相关性和快速傅里叶变换（FFT）分析的策略来降低背景噪声。
   - **相干平均技术**：通过多次采样来提高信噪比。
   - **小波去噪技术**：消除低频噪声和其他心音干扰。
   - **独立成分分析（ICA）**和**自适应最小均方（LMS）方法**：用于噪声消除和自适应滤波。
   - 使用不同滤波器，如**Butterworth带通滤波器**和**椭圆滤波器**，来获得所需的心音频带。

3. **信号分割方法**：
   - 使用**Hidden Markov Model (HMM)**和**Hidden Semi-Markov Model (HSMM)**进行分割。
   - 通过**自相关函数（ACF）**检测周期性成分并估计收缩期长度。
   - 使用**矩形窗口**分割每个心动周期。

4. **特征提取和分类技术**：
   - 从分割后的PCG信号中提取特征，包括统计频率域和时域特征。
   - 应用不同的分类算法，如**支持向量机（SVM）**、**卷积神经网络（CNN）**、**长短期记忆网络（LSTM）**和**人工神经网络（ANN）**。
   - 一些研究中使用的特定技术包括**Mel频率倒谱系数（MFCCs）**、**离散小波变换特征**、**自回归（AR）模型**、**独立成分分析（ICA）**和**线性预测编码（LPC）参数**。

5. **研究结果**：不同的研究通过利用不同的特征和分类技术，实现了不同程度的分类准确率，例如：
   - SVM和LSTM分别达到了78.64%和74.9%的准确率。
   - 通过集成不同的特征和分类器，如SVM、决策树（DT）、概率神经网络（PNN）等，得到了94.60%、82.62%和84.54%的准确率。
   - 通过CNN、随机森林（RF）和SVM在MFCCs上实现了81.3%、74.2%和75.6%的准确率。
   - 使用RNN得到了74.9%的准确率。
   - ANN和kth-最近邻（KNN）分别达到了93.3%和98.8%的准确率。

6. **研究目的**：设计智能系统以辅助诊断，提高心音信号分类的准确性，帮助心脏病专家进行决策。

7. **研究方法**：使用2016年PhysioNet/Computing in Cardiology Challenge的公开数据库，包含超过3000个心音记录，进行PCG分析和分类模型的训练。

8. **论文结构**：包括方法论、数据库和特征集描述、性能评估指标、实验结果、比较分析以及结论和未来工作机会。

综述强调了心音信号分析在心血管疾病诊断中的重要性，并概述了提高心音信号分类准确性的不同技术和方法。



### 2. METHODOLOGY

#### 2.1. Segmentation
采用的分割技术为由 Springer 等(2015)提出的基于 LR-HSMM 的心音分割用于 PCG 分割，这是 Schmidt 等(2010)提出的与持续时间相关的 HMMl 的扩展版本。

#### 2.2. Feature extraction
In the first type, both time and frequency features are extracted using segmented PCG signals, as listed in table 3. 对分割后的数据，提取50个时域和频域特征。
In the second type, MFCCs are computed using the unsegmented PCG signal; details are given in the following subsections.对于未分割的数据，提取MFCC特征。

##### 2.2.1. Time and frequency domain features
对分割后的数据，提取50个特征，20个时域和30个频域特征。
50 features (20 time-domain and 30 frequency-domain) are extracted from the segmented PCG signal, as listed in table 3.

![表3](01图片/03表3.png)

##### 2.2.2. Mel-frequency cepstral coefficients

#### 2.3. Classification
本研究使用了六种不同的分类算法，包括支持向量机、第k近邻、决策树、人工神经网络、长短期记忆网络和集成分类器。使用不同的时域和频域特征，利用分段和未分段的PCG信号来评估每个分类器的性能。表IV说明了用于训练和测试每个分类器的记录。

![表4](01图片/04表4.png)

> 下面作者介绍了不同的分类器，不看也罢。因为，第一，作者既没有提供更加详细的操作方法，他没说，他使用R还是Python呐，也没说具体的代码是啥，第二，也没有详细且生动形象地以一种让人通俗易懂的方式介绍这些分类器，所以，这一部分，不看也罢，全是废话。简而言之，不讲实操，理论知识介绍的也不详细。

##### 2.3.1. Support vector machine

![图3](01图片/03图3.png)

##### 2.3.2. Nearest neighbors
![图4](01图片/04图4.png)

##### 2.3.3. Decision tree
![图5](01图片/05图5.png)

##### 2.3.4. Ensemble classifier
![图6](01图片/06图6.png)

##### 2.3.5. Artificial neural network
![图7](01图片/07图7.png)

##### 2.3.6. Long short-term memory networks

LSTM 是 RNN 的一个变体，已被广泛用于序列数据的分析。与其他使用前馈连接的神经网络不同，LSTM 有一个反馈连接，有助于在预测期间记住先前的输入以及最近对当前输出的输入(Hochreiter et al 1997)。RNN 的缺点是每次迭代后梯度都呈指数衰减，导致有用信息的丢失(Arbel 2018)。另一方面，LSTM 利用“记忆细胞”和多个重复的乙状结肠和 Tanh 作为激活层来决定长期忘记和记忆什么(Kang et al 2017)。LSTM 比 RNN 有更多的内存，这使得它适合于学习长时间的输入。LSTM 的单元被用作 RNN 层的构建模块，这就是为什么它经常被称为 LSTM 网络。LSTM 中有三个门: 输入门、忘记门和输出门，如图8所示。门决定了以下几个方面: 输入门决定是否允许网络中的新输入，如果信息不重要，则忘记门监视并删除该信息，输出门控制当前时间步骤中数据对输出的影响。这些门是模拟的，允许执行反向传播机制。


> LSTM的优点讲了，那缺点呢？

![图8](01图片/08图8.png)

### 3. PCG DATABASE, FEATURE SETS AND PERFORMANCE EVALUATION METRICS

![表4](01图片/04表4.png)

总样本为3126，其中80%为训练集，20%为测试集。
换言之，2501个样本用于训练，625个样本用于测试。







在过去的研究中，根据广泛的文献综述(Maglogiannisa 和 Loukis 2009，Bouril 和 Aleinikava 2016，Homsi 和 Medina 2016)提取不同的时间和频率域特征，并创建集 A，包含所有50个特征(20个时间域和30个频率域)。
* 随后，将集合 A 分解为集合 B 和集合 F，分别包含20个时域特征和30个频域特征。

在不同的机器学习分类器上使用所选择的特征子集进行了不同的实验。

之后，还采用了其他特征选择标准。例如，通过使用 ANOVA 分析和具有不同 k 值的“ best-k”特征选择方法生成特征集，然后计算每个特征和用于选择特征的目标向量之间的 F 值。

这里报道了一些有意义的特征子集。

* 在集合 C 中，选择得分最高的28个特征，(这里的集合C，我猜测来源于集合A)
* 同样地，集合 E 使用 k = 18创建。
* 随后，通过从集 F 中选择“最佳 k”特征来创建集 D，其中选择了26个频域特征。
* 最后，集合 G 由前13个 MFCC 创建。



通过对所创建子集的初步实验(啥实验啊，源代码呢？)，发现分类器在集合 A、 B、 C 和 F 上的性能优于集合 D 和 E，因此，集合 D 和集合 E 通过使用集合 F 和集合 A 的前8个特征替换8个特征进行修改，使用“最佳-k”选择方法。
> 我觉得替不替换，其实问题不大，除非替换后的集合 D 和集合 E 的性能优于集合 F 和集合 A 



给出了用于评估该方法的性能评估指标如下。对于每个分类器和不同的特征集，分类结果记录为真阳性(TP) ，真阴性(TN) ，假阳性(FP)和假阴性(FN)的平均10次迭代，这些迭代进一步用于计算灵敏度，也称为真阳性率(TPR)。TPR、特异性、准确性(假阳性率(FPR))、总分和曲线下面积(AUC)

![09评价指标](01图片/09评价指标.png)


![09图9ROC曲线](01图片/09图9ROC曲线.png)

### 4. Experiments
该部分将实验分析分为分段 PCG 数据分析和非分段 PCG 数据分析三个部分，然后将提出的特征集与其他相关特征和分类器进行比较。


#### 4.1. Segmented data analysis
在分段数据分析中，首先使用 Springer (2015)分割方法进行 PCG 分割。然后提取时间和频率特征，如表3所示。在所提出的研究中，提出了不同的特征集，这些特征集将在第3节中讨论。然后，利用这些提取的特征训练不同的分类模型。表5列出了分类器的规格(SVM、 KNN、 DT、 LSTM、 ANN 和集成)。对于每个分类器，通过经验确定合适的特征集。PCG 数据库的详细信息和培训测试策略在第3部分进行了讨论。对不同的分类器和特征数量的变化进行了全面的实验，并记录了不同特征集(在分类性能方面)的影响。所获得的结果总结在表6中，并在下面进行了讨论。



![表5紧凑版](01图片/05表5紧凑版.png)

表5概述了不同类型的分类器及其具体参数和配置：

##### SVM（支持向量机）
- 标准化：是（True）
- 核函数：Rbf（径向基函数）
- 核比例：自动（Auto）

##### K-NN（k近邻）
- 标准化：是（True）
- 邻居数量：未提供具体数值

##### 决策树
- 最大分裂数：75
- 分裂标准：Gini多样性指数
- 替代决策分裂：关闭（OFF）
- 每个节点的最大替代数：10

##### LSTM（长短期记忆网络）
- 输入层激活函数：Relu
- LSTM单元数：32
- 输出层激活函数：Softmax
- 优化器：随机梯度下降（SGD）
- 学习率：0.01

##### ANN（人工神经网络）
- 输入层激活函数：Relu
- 输出层激活函数：Softmax
- 优化器：Adam
- 隐藏层数量：6
- 神经元数量：46
- 学习率：0.0001

##### 集成分类器
- 学习器类型：树（Tree）
- 学习类型：装袋（Bagging）
- 模型数量：100

请注意，K-NN的“Number of neighbors”（邻居数量）参数在表格中没有给出具体数值。其他分类器的参数都已详细列出，包括激活函数、优化器、学习率、隐藏层数量和神经元数量等。


![表6](01图片/06表6紧凑版.png)

表6概述了使用不同方法和特征集对分割心音图（PCG）进行分类的结果：

##### 分类结果概览

| 方法 | 特征集 | 敏感性(%) | 特异性(%) | 准确性(%) | AUC(%) |
|------|--------|------------|------------|------------|---------|
| SVM  | set D  | 57.06      | 96.11      | 88.15      | 76.59    |
|      | set A  | 67.70      | 95.83      | 88.71      | 81.77    |
|      | set F  | 53.80      | 96.18      | 87.48      | 74.99    |
| K-NN | set F  | 48.58      | 96.64      | 87.05      | 72.61    |
|      | set A  | 47.84      | 96.40      | 86.33      | 72.12    |
|      | set E  | 48.49      | 95.89      | 86.14      | 72.19    |
| DT   | set A  | 63.78      | 91.62      | 85.77      | 77.70    |
|      | set C  | 64.17      | 91.99      | 86.11      | 78.08    |
|      | set D  | 64.77      | 191.85     | 86.35      | 78.31    |
| ANN  | set A  | 43.86      | 95.89      | 79.23      | 69.87    |
| LSTM | set A  | 47.48      | 99.79      | 78.59      | 73.64    |
| Ensemble Classifier | set D  | 78.81      | 97.04      | 91.23      | 87.92    |
|      | set F  | 68.14      | 96.86      | 90.78      | 82.50    |


**注意**:
- 敏感性（Sensitivity）表示模型正确识别正类（如心脏病例）的能力。
- 特异性（Specificity）表示模型正确识别负类（如健康心音）的能力。
- 准确性（Accuracy）是模型整体正确分类的比例。
- AUC（Area Under the Curve）是接收者操作特征曲线下面积，衡量分类器性能的一个指标。


**方法说明**:
- SVM、K-NN、DT、ANN、LSTM和集成分类器是使用的分类方法。


**性能比较**:
- 集成分类器在特征集D上表现最好，具有最高的准确性和AUC。
- SVM和K-NN在特征集A上的表现相对较好。
- ANN和LSTM在特征集A上的表现不如其他方法。

这个表格提供了不同分类器和特征集组合的性能比较，可以用来评估哪种组合最适合心音信号的分类任务。



##### SVM
首先对 SVM 分类器进行频域特征训练和测试(参见 F 集，只有30个频域特征) ，得到了87.48% 的准确率。
在此基础上，增加了20个时域特征，使识别率提高到88.71% (即集合 A，有20个时域特征和30个频域特征)。

##### KNN
在 KNN 的情况下，采用不同的时间和频率特征组合。最初，通过使用全部50个特性(参见集 A，有20个时域特征和30个频域特征) ，准确率为86.33% ; 
然而，使用特性集 F(参见 F 集，只有30个频域特征) ，准确率增加到87.05% 。

##### DT
对于 DT，
集合 A 的分类准确率为85.77% ，
集合 C 的分类准确率为86.11% ，
集合 D 的分类准确率为86.35% 。

##### 集成分类器
集成分类的准确率最高，集合 D 和集合 F 的准确率分别为91.23% 和90.78% 。

##### ANN 和 LSTM
此外，集合 A 的人工神经网络和 LSTM 的分类准确率分别为79.23% 和78.59% 。

尽管在某些情况下其他特性集表现良好，但总体而言，集 A 的特性表现出更好的性能，并被认为更有用。

#### 4.2. Unsegmented data analysis
本节给出了无分段 PCG 数据分析的实验细节和结果。对于未分段的 PCG 分析，MFCC 特征是从所有3126个心音录音中提取出来的，并用表5中列出的规格进行分类器训练。


![表5](01图片/05表5紧凑版.png)



所得结果见表7。
结果表明，在准确性方面，所有分类器都能很好地利用 MFCC 特征(特别是达到75% 以上的准确率)。
然而，支持向量机和集成分类器的分类准确率分别为81.14% 和81.63% ，优于其他分类器。
在 AUC 分数方面，观察到 LSTM 优于所有其他分类算法，达到了91.39% 的 AUC 分数。

![表7](01图片/07表7.png)

表7概述了使用不同方法对未分割心音图（PCG）进行分类的结果：

##### 未分割PCG分类结果概览

| 方法 | AUC(%) | 敏感性(%) | 特异性(%) | 准确性(%) |
|------|--------|------------|------------|------------|
| SVM  | 15.70  | 97.92      | 81.14      | 56.81      |
| DT   | 39.39  | 84.78      | 75.57      | 62.08      |
| ANN  | 17.36  | 95.24      | 80.19      | 56.30      |
| KNN  | 11.70  | 97.13      | 79.67      | 54.42      |
| LSTM | 83.24  | 99.55      | 80.68      | 91.39      |
| 集成分类器 | 23.21  | 96.97      | 81.63      | 60.09      |

**说明**:
- AUC（Area Under the Curve）是接收者操作特征曲线下面积，衡量分类器性能的一个指标。
- 敏感性（Sensitivity）表示模型正确识别正类（如心脏病例）的能力。
- 特异性（Specificity）表示模型正确识别负类（如健康心音）的能力。
- 准确性（Accuracy）是模型整体正确分类的比例。

**性能比较**:
- LSTM方法在未分割的PCG信号分类中表现最好，具有最高的准确性和AUC值。
- KNN和SVM虽然具有很高的敏感性，但在准确性和AUC上的表现不如LSTM。
- 决策树（DT）在特异性上表现不错，但AUC值较低，表明其整体性能有限。
- 集成分类器的AUC值不高，但其敏感性和特异性相对较好。

这个表格提供了不同分类器在未分割心音信号分类任务中的性能比较，可以用来评估哪种分类器最适合处理未分割的心音信号数据。









#### 4.3. Comparison of segmented and unsegmented analysis

总之，实验观察到，总的来说，频率特征在分类中是有用的。

本文讨论了分段和非分段 PCG 数据分类结果的比较。

从表6的结果可以推断，即使只加入少量的频域特征，也可以获得更高的分类精度，例如，支持向量机分类器仅利用26个频域特征(集 D)就可以获得88.15% 的分类精度。

![表6](01图片/06表6紧凑版.png)


此外，观察到与使用未分段 PCG 的 MFCC 特征训练的分类器相比(见表7) ，使用分段 PCG 数据特征的分类器的性能更好。
![表7](01图片/07表7.png)

这可能是由于许多因素，包括 MFCC 在 PCG 分析中的不适用性(Devi 等，2015，Ismail 等，2018)。



适当的分割对提取有用的特征和提高分类器性能也有重要作用。此外，由于表3中提出的时间/频率特征通常是基于心脏周期中的分段间隔，分段中的任何一致性都有助于提高分类的准确性。




#### 4.4. Comparison with other methods
最后，将提出的特征集的性能与同一数据集上的其他相关工作进行了比较。

在这组实验中，我们只考虑集合 A 特征，并且得到了不同分类器的分类性能。

与 Bouril 和 Aleinikava (2016)使用平衡数据集(即相同数量的正常和异常记录(各349次)和不同的特征集取得的结果相比，我们的结果通过利用不平衡的数据集(即超过3000次记录的不平等数量的正常和异常记录)显着改善，如表8所示。

![表8](01图片/08表8.png)

表8比较了不同研究工作中使用的技术、特征集、以及它们在心音图（PCG）分类任务上的性能：

##### 与先前工作的比较

| 序号 | 引用 | 分类技术 | 特征集 | 敏感性(%) | 特异性(%) | 总体得分(%) | 准确性(%) |
|------|------|----------|----------|------------|------------|-------------|------------|
| 1    | Christopher Sch'olzel, 2016 | LSTM | 提出的特征（set A） | 47.48 | 99.79 | 73.63 | - |
| 2    | Anthony Bouril, 2016 | SVM | 统计特征 | 73.3 | 83.98 | 78.64 | - |
| 3    | Iga Grzegorczyk, 2016 | 神经网络 | 统计特征 | 81.0 | 76.0 | 79.0 | - |
| 4    | Masun Nabhan, 2016 | 集成分类器 | 统计特征和5级小波分解生成的特征 | 88.48 | 80.48 | 84.48 | - |
| 5    | Roilhi Ibarra-Hernandez, 2018 | KNN | 提出的特征（set A） | 47.84 | 96.40 | 86.33 | 60.63 |
| 6    | 当前研究 | DT（决策树） | 提出的特征（set A） | 63.78 | 91.62 | 85.77 | - |

**说明**:
- 敏感性（Sensitivity）表示模型正确识别正类（如心脏病例）的能力。
- 特异性（Specificity）表示模型正确识别负类（如健康心音）的能力。
- 总体得分（Overall Score）可能是基于敏感性和特异性计算的一个综合评分。
- 准确性（Accuracy）是模型整体正确分类的比例，某些研究提供了这一数据，而其他研究则没有。

**性能比较**:
- 在比较的工作中，Masun Nabhan等人使用的集成分类器在总体得分上表现最好，达到了84.48%。
- Christopher Sch'olzel等人使用的LSTM技术在特异性上表现突出，达到了99.79%。
- Anthony Bouril等人使用的SVM技术在敏感性和特异性上取得了较好的平衡，总体得分为78.64%。
- 当前研究提出的决策树（DT）技术在准确性上表现良好，达到了85.77%。

这个表格提供了不同研究在心音信号分类任务中的性能比较，可以用来评估不同特征集和分类技术的有效性。


此外，当使用我们提出的特征代替 LPC 参数(Ibarra-Hernández 和 Bertin 2018)时，KNN 显示了相对更好的结果。

同样，与 Homsi 和 Medina (2016)提出的三个集成分类器(RF，LogitBoost (LB)和成本敏感分类器(CSC))相比，具有100个树学习者的集成分类器表现更好。

基于神经网络的深度学习算法也被广泛应用于异常心音的检测。
与经典的机器学习算法相反，在本研究中，如 Schölzel 和 Dominik 2016所提出的，LSTM 和 ANN 通过减少 LSTM 单元(即只有32个具有软最大激活输出层的 LSTM 单元)表现良好。
此外，提出的神经网络结构的修改导致更好的分类性能(特别是六个隐藏层，包括两个辍学层以避免过度拟合，总共有46个神经元，而不是 Grzegorczyk 和 Soli ski (2016)提出的两个隐藏层59个神经元)。
此外，还对使用 DT 分类器的分类性能进行了评估，据我们所知，这是首次使用在生理网络/挑战数据集上，准确率为86.35% ，总分为78.31% ，比 KNN 和 LSTM 相对更好。


### 5. Conclusions and future work
在这项研究中，使用了来自最大的公开可用数据库“2016年的生理网络/挑战”的3000多个心音，并进行了各种实验。本研究以实验为主，旨在对 PCG 资料有新的认识，进而提高分类的准确性。表6和表7中的结果是基于 AUC 性能指标进行评估的，我们的结论总结如下：


* 1.利用未分段数据的 MFCC 特征和 LSTM 分类器获得最佳分数。该组合优于其他算法，AUC 得分为91.39% 。
* 2.考虑到分段 PCG 数据的时间和频率特征子集，我们发现集合 A (包含所有50个特征)更适合于训练机器学习算法。因为所有的分类器在这个集合上表现良好，并且获得了超过70% 的 AUC 分数。
* 3.考虑到使用分段数据的不同时间/频率特征的分类性能，除 LSTM 以外的所有分类器都表现得更好，集合分类器的 AUC 得分达到87.92% ，优于其他分类器。
* 4.总的来说，在特征方面，我们发现分段 PCG 的时间和频率特征组合比未分段 PCG 的 MFCC 更适合于 PCG 分析。(**如果我既分段，又提取MFCC特征，模型性能会更好吗？**)
* 5.同样，在数据方面，分类算法在分段 PCG 上表现出一致的性能。
* 6.实质上，由于 MFCC 与 LSTM 分类器之间的高度相关关系，LSTM 在 MFCC 上表现更好，这被认为在 DNN 中更适合于连续数据(Latif 等，2018)。**因此，LSTM 只能在非分段数据的 MFCC 特征上表现出非凡的性能**，而其他传统的机器学习技术在不同的特征子集上表现出一致的性能。


未来，作者打算开发一个 PCG 信号数据库，并将应用深度学习分类方法，包括神经网络和双向 LSTM。此外，还将研究希尔伯特变换、离散小波变换和 PSD 等新特性。






































































































































































